from pathlib import Path

# ============================================================
# 项目根目录路径
# 通过当前文件所在目录的父目录（common/）的父目录来确定项目根目录
# 所有模块引用项目路径时统一使用此变量，避免硬编码路径
# ============================================================
BASE_DIR = Path(__file__).resolve().parent.parent
